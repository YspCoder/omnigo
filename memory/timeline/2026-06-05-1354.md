# Timestamp
2026-06-05 13:54

# Task
继续补齐 pai 的查询与 modify 能力

# What Changed
- 为 `pai` 增加 `restyle/list` 的统一查询封装，接入 `ListTasks`
- 新增 `modify` 能力的请求组装与 mode 识别
- 为 `modify` 增加图片上传与数组字段处理
- 扩展测试覆盖 `restyle/list` 与 `modify`
- README 补充 `restyle/list` 与 `modify` 的当前状态说明

# Files
- adapter/pai.go
- adapter/pai_test.go
- README.md
- memory/timeline/2026-06-05-1354.md
- memory/summaries/current-state.md
- memory/architecture/provider-pai.md
- memory/tasks/current.md

# Reason
把 Pai 剩余最关键的一组可用能力补齐，让查询型和编辑型接口也能落入统一访问层。

# Important Decisions
- `restyle/list` 通过 `ListTasks(mode=restyle)` 暴露，而不是单独新增自定义公开 API
- `modify` 先按官方 developing 文档接入，但在 memory 与 README 中明确标注验证风险
- `modify` 中的 `img_ids`、`mask_ids`、`keyframe_ids` 保留多值能力，不退化成单值写法

# Risks
- `restyle/list` 当前映射到 `TaskListResponse`，语义是“可用风格项”而不是后台任务列表
- `modify` 仍然依赖调用方准备符合 prompt 占位符约定的 `img_ids` / `mask_ids` / `keyframe_ids`
- 官方若调整 developing 接口字段，`modify` 最容易漂移

# Next Possible Steps
- 为 Pai 增加真实环境 smoke test
- 如果 `modify` 线上验证稳定，再补示例代码
- 继续抽离任务型视频 provider 通用层
