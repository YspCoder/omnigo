# Timestamp
2026-06-05 13:25

# Task
继续为 pai 接入剩余独立能力

# What Changed
- 新增 `mask-selection`、`sound-effect`、`restyle` 三类 Pai 能力
- `mask-selection` 接为同步辅助能力，并把原始结构化结果保留在响应文本中
- `swap` 支持 `auto_mask_selection=true` 自动先调 mask 选择接口
- 补充相应单元测试，并更新 README 使用说明

# Files
- adapter/pai.go
- adapter/pai_test.go
- README.md
- memory/timeline/2026-06-05-1325.md
- memory/summaries/current-state.md
- memory/architecture/provider-pai.md
- memory/tasks/current.md

# Reason
继续补完 Pai 官方文档中的任务型与辅助型独立能力，降低调用方手工编排成本。

# Important Decisions
- `mask-selection` 不强行塞进 `TaskStatus` 语义，改为同步 `Media` 响应返回
- `swap` 自动 mask 只做“选首个 mask”的轻封装，复杂选择仍由调用方决定
- `restyle/list` 暂未做统一封装，先接核心生成接口

# Risks
- `mask-selection` 当前把结构化结果放在 `MediaResponse.Text`，调用方需要自行解析
- `auto_mask_selection` 默认取首个 mask，不适合需要精确指定对象的场景
- `modify` 仍处于官方 developing 状态，未接入

# Next Possible Steps
- 为 `restyle/list` 提供统一查询封装
- 评估是否为 `mask-selection` 单独抽一个 typed helper
- 继续清理 Pai 内部模式判断和媒体上传重复逻辑
